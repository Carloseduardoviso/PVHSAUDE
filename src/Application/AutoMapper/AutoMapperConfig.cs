using AutoMapper;

namespace PVHSAUDE.Application.AutoMapper
{
    public class AutoMapperConfig : Profile
    {
        public AutoMapperConfig()
        {
            //CreateMap<Sistema, SistemaVm>().MaxDepth(2).ReverseMap().PreserveReferences();
        }
    }
}